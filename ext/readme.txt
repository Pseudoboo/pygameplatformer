This folder is meant to contain logic for interacting with information that is non-deterministic that is external to the system. This folder will have interface definitions, while the folder "impl" will have usable implementations of code. Any test implementations will have the word 'stub' in the name.